fragment traderFields on Trader {
  id
  PNL
  depositAmount
  totalTrades
  totalVolume
  totalRevenue
  totalPremium
  totalExercisedOptionPosition
  updateTimestamp
}

query getTraders {
  traders(first: 50, orderBy: PNL, orderDirection: desc) {
    ...traderFields
  }
}
